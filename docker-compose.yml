version: "3"

services:
  unbound:
    network_mode: host
    container_name: unbound
    image: ghcr.io/mfbernardes/docker-unbound:latest
    cap_drop:
      - ALL
    cap_add:
      - SETGID
      - SETUID
    environment:
      ENABLE_IPV6: "yes"
    restart: unless-stopped
